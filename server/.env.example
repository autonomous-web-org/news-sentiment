# --- GitHub repo settings (for CSV updates) ---
GH_OWNER=autonomous-web-org
GH_REPO=news-sentiment
GH_BRANCH=main

# Fine-grained PAT or classic PAT with repo/contents write; keep secret
GITHUB_TOKEN=YOUR_GITHUB_TOKEN_WITH_CONTENTS_WRITE

# --- Tickers source JSON (read-only) ---
REPO_JSON_URL=https://raw.githubusercontent.com/autonomous-web-org/news-sentiment/refs/heads/main/src/assets/data/tickers_last_updated.json
# Path inside the repo to update (JSON advancement)
TICKERS_JSON_PATH_IN_REPO=src/assets/data/tickers_last_updated.json

# --- Gemini API (free key works) ---
# Set one of these; client auto-detects if present
GEMINI_API_KEY=YOUR_FREE_OR_PAID_GEMINI_API_KEY
# Optional: override model; 1.5-flash is fast and free-tier friendly
GEMINI_MODEL=gemini-1.5-flash

# --- Optional tuning ---
#LOG_LEVEL=INFO
#TZ=UTC
